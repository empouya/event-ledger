# Test-only dependencies for tests/e2e/ (pytest + boto3 against LocalStack).
# Install into the project's Python 3.12 environment:
#   pip install -r requirements-test.txt
pytest>=8.0
boto3>=1.34
PyJWT>=2.8
requests>=2.31
