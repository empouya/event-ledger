AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31

Description: StreamCore event-processing pipeline


# Globals apply default settings to ALL functions/APIs in this template.
# This avoids repeating the same config on every resource.
Globals:
    Function:
        Runtime: python3.12
        Architectures:
        - x86_64
        Tags:
            Project: StreamCore
            Environment: !Ref Environment


# Parameters let you pass different values per environment (dev/staging/prod)
# without changing the template itself.
Parameters:
    Environment:
        Type: String
        Default: dev
        AllowedValues: [dev, staging, prod]
    ShardCount:
        Type: Number
        Default: 1
        Description: >
            Number of Kinesis shards. 1 shard handles up to 1,000 events/sec write
            or 1 MB/sec write. Keep at 1 for LocalStack; increase for real AWS
            when approaching those throughput limits.

    RetentionPeriodHours:
        Type: Number
        Default: 24
        Description: >
            How long Kinesis retains events (hours). The spec requires 168h (7 days)
            in production for replay guarantees. Using 24h locally because LocalStack
            does not enforce retention and this keeps the local/real split honest.


# Resources is where all AWS infrastructure is declared.
Resources:
    # ── KMS Customer Managed Key ──────────────────────────────────────────────
    # One CMK for the whole pipeline. Annual rotation is mandatory (SEC-ENC-02):
    # KMS automatically creates a new key version each year and keeps old
    # versions available for decryption — so existing ciphertext never breaks.
    #
    # Key policy uses the "account-root enables IAM" pattern:
    #   • The root principal grants kms:* to the account — this does NOT give
    #     everyone access; it just allows IAM policies to control access.
    #   • Actual per-role grants (kms:Decrypt / kms:GenerateDataKey) live in
    #     each role's IAM policy (see ConsumerFunction below).
    #   • This avoids a CloudFormation circular dependency: if the key policy
    #     listed role ARNs, CFN would need the roles to exist before creating
    #     the key, but the roles reference the key — a deadlock.
    PipelineKey:
        Type: AWS::KMS::Key
        Properties:
            Description: !Sub "StreamCore pipeline CMK — ${Environment}"
            EnableKeyRotation: true   # Annual automatic rotation (SEC-ENC-02)
            KeyPolicy:
                Version: "2012-10-17"
                Statement:
                    - Sid: EnableIAMPolicies
                      Effect: Allow
                      Principal:
                          AWS: !Sub "arn:aws:iam::${AWS::AccountId}:root"
                      Action: "kms:*"
                      Resource: "*"
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # Human-readable alias for the CMK.
    # Using an alias means we can reference alias/streamcore-pipeline-key
    # in the console and CLI without knowing the key ID.
    PipelineKeyAlias:
        Type: AWS::KMS::Alias
        Properties:
            AliasName: alias/streamcore-pipeline-key
            TargetKeyId: !Ref PipelineKey

    # The API Gateway that exposes HTTP endpoints to the internet.
    # SAM creates and manages the underlying API Gateway resource automatically.
    StreamCoreApi:
        Type: AWS::Serverless::Api
        Properties:
            StageName: !Ref Environment   # URL will be: .../dev/health
            Auth:
                Authorizers:
                    # JwtAuthorizer is wired only to POST /v1/events (see below).
                    # GET /health is left open -- no DefaultAuthorizer set.
                    JwtAuthorizer:
                        FunctionArn: !GetAtt AuthorizerFunction.Arn
                        FunctionPayloadType: REQUEST
                        Identity:
                            Headers:
                                - Authorization
                        AuthorizerResultTtlInSeconds: 0

    # The Lambda function that handles GET /health requests.
    HealthFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/health/    # Folder SAM packages and uploads to S3
            Handler: handler.lambda_handler  # filename.function_name
            Description: Returns HTTP 200 to confirm the API and Lambda are operational
            Environment:
                Variables:
                    # !Ref on a DynamoDB table returns the table name.
                    # Injected here so the handler never hardcodes it.
                    TABLE_NAME: !Ref EventsTable
            Policies:
                - Statement:
                    - Effect: Allow
                      Action: dynamodb:DescribeTable
                      Resource: !GetAtt EventsTable.Arn
            Events:
                HealthGet:
                    Type: Api
                    Properties:
                        RestApiId: !Ref StreamCoreApi
                        Path: /health
                        Method: GET
    # Lambda REQUEST authorizer -- validates HS256 Bearer JWTs before any
    # Lambda on POST /v1/events is invoked.  Local mock for Cognito (ADR-009).
    #
    # FunctionPayloadType: REQUEST means API Gateway passes the full request
    # context (headers, path, method) to this function, not just the token.
    # This lets the authorizer inspect the Authorization header directly.
    #
    # AuthorizerResultTtlInSeconds: 0 -- disable result caching so every
    # request is validated fresh.  Important for local testing and for
    # exercising the 401/403 paths without cache interference.
    AuthorizerFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/authorizer/
            Handler: handler.lambda_handler
            Description: Validates HS256 Bearer JWT; returns IAM Allow/Deny policy
            Timeout: 5
            MemorySize: 128
            Policies:
                - Statement:
                    - Effect: Allow
                      Action: secretsmanager:GetSecretValue
                      Resource: !Sub "arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:streamcore/jwt-secret*"

    # Ingest Lambda — hot-path entry point for POST /v1/events.
    # Receives the event body, stamps it with tenantId/ingestionId/ingestedAt,
    # and writes one Kinesis record. Must be lightweight — no heavy logic here.
    IngestFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/ingest/
            Handler: handler.lambda_handler
            Description: Accepts POST /v1/events, injects ingestion metadata, writes to Kinesis
            Timeout: 10         # seconds — well above p99 even under load
            MemorySize: 256     # MB — sufficient for a simple Kinesis PutRecord call
            Environment:
                Variables:
                    # Stream name from CloudFormation — never hardcoded.
                    # !Ref on a Kinesis stream returns its name (not ARN).
                    EVENT_STREAM_NAME: !Ref EventStream
            Policies:
                # Least-privilege: only the one action this function actually calls.
                - Statement:
                    - Effect: Allow
                      Action: kinesis:PutRecord
                      Resource: !GetAtt EventStream.Arn
            Events:
                IngestPost:
                    Type: Api
                    Properties:
                        RestApiId: !Ref StreamCoreApi
                        Path: /v1/events
                        Method: POST
                        Auth:
                            Authorizer: JwtAuthorizer

    # Consumer Lambda — reads from Kinesis in batches and writes to DynamoDB.
    # This is the only writer to EventsTable; the Ingest Lambda never touches DynamoDB.
    #
    # BatchSize 100 / window 5s: Kinesis will call this function when it has
    # 100 records OR 5 seconds have passed, whichever comes first.
    # BisectBatchOnFunctionError: on any unhandled exception, Kinesis splits the
    # batch in half and retries each half separately — isolating bad records
    # without replaying the entire batch from scratch.
    ConsumerFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/consumer/
            Handler: handler.lambda_handler
            Description: Decodes Kinesis records and starts one Step Functions execution per event
            Timeout: 60
            MemorySize: 256
            # KmsKeyArn tells Lambda to encrypt this function's environment
            # variables at rest using our CMK rather than the default
            # Lambda-managed key. The execution role must have kms:Decrypt
            # so Lambda can decrypt the vars on each cold start.
            # Only ConsumerFunction needs this in Phase 4.1: STATE_MACHINE_ARN
            # is the only env var that contains an account-ID ARN (SEC-ENC-04).
            KmsKeyArn: !GetAtt PipelineKey.Arn
            Environment:
                Variables:
                    STATE_MACHINE_ARN: !Ref ProcessingStateMachine
            Policies:
                - Statement:
                    - Effect: Allow
                      Action: states:StartExecution
                      Resource: !Ref ProcessingStateMachine
                    # Required for the ESM on-failure destination.
                    # Lambda service writes the failure envelope to this queue
                    # on behalf of this function's execution role.
                    - Effect: Allow
                      Action: sqs:SendMessage
                      Resource: !GetAtt ValidationDLQ.Arn
                    # kms:Decrypt — Lambda needs this to decrypt the CMK-
                    # encrypted environment variables (STATE_MACHINE_ARN)
                    # at cold-start time.
                    # kms:GenerateDataKey — needed to re-encrypt after a
                    # key rotation event.
                    - Effect: Allow
                      Action:
                          - kms:Decrypt
                          - kms:GenerateDataKey
                      Resource: !GetAtt PipelineKey.Arn
            Events:
                KinesisStream:
                    Type: Kinesis
                    Properties:
                        Stream: !GetAtt EventStream.Arn
                        StartingPosition: LATEST
                        BatchSize: 100
                        MaximumBatchingWindowInSeconds: 5
                        # BisectBatchOnFunctionError: if the Lambda crashes entirely,
                        # Kinesis bisects the batch and retries each half — second
                        # line of defence after ReportBatchItemFailures.
                        BisectBatchOnFunctionError: true
                        # ReportBatchItemFailures: the Lambda returns
                        # {"batchItemFailures": [...]} so only failing records
                        # are retried, not the whole batch.
                        FunctionResponseTypes:
                            - ReportBatchItemFailures
                        # After 2 retries (3 total attempts), exhausted records
                        # go to the on-failure destination below.
                        MaximumRetryAttempts: 2
                        DestinationConfig:
                            OnFailure:
                                Type: SQS
                                Destination: !GetAtt ValidationDLQ.Arn

    # ── Step Functions execution role ────────────────────────────────────────
    # SEC-AUTH-04: dedicated named role for the state machine.
    # Named so auditors can find it in the console without decoding a
    # CloudFormation-generated suffix.
    #
    # Permissions (three Sids):
    #   InvokeLambda       -- the four pipeline Lambdas, nothing else
    #   CloudWatchLogs     -- nine log-delivery actions; Resource: * is mandated
    #                         by AWS (none of these support resource constraints)
    #   DLQWrite           -- sqs:SendMessage on the two DLQ ARNs only
    #
    # SEC-AUTH-04 deviation: the spec says "only lambda:InvokeFunction, no data
    # access". The ASL sends validation and persistence failures directly to SQS
    # via arn:aws:states:::sqs:sendMessage, so sqs:SendMessage is unavoidable
    # without redesigning DLQ routing into a Lambda wrapper (higher cost/latency).
    # Scoped to exactly ValidationDLQ and PersistenceDLQ. Logged in worklog.
    StreamCoreStepFunctionsRole:
        Type: AWS::IAM::Role
        Properties:
            RoleName: !Sub "StreamCoreStepFunctionsRole-${Environment}"
            Description: !Sub "Step Functions execution role for StreamCore pipeline - ${Environment}"
            AssumeRolePolicyDocument:
                Version: "2012-10-17"
                Statement:
                    - Effect: Allow
                      Principal:
                          Service: states.amazonaws.com
                      Action: sts:AssumeRole
            Policies:
                - PolicyName: PipelineStateMachinePolicy
                  PolicyDocument:
                      Version: "2012-10-17"
                      Statement:
                          - Sid: InvokeLambda
                            Effect: Allow
                            Action: lambda:InvokeFunction
                            Resource:
                                - !GetAtt ValidatorFunction.Arn
                                - !GetAtt PiiExtractorFunction.Arn
                                - !GetAtt EnricherFunction.Arn
                                - !GetAtt WriterFunction.Arn
                          - Sid: CloudWatchLogDelivery
                            Effect: Allow
                            Action:
                                - logs:CreateLogDelivery
                                - logs:GetLogDelivery
                                - logs:UpdateLogDelivery
                                - logs:DeleteLogDelivery
                                - logs:ListLogDeliveries
                                - logs:PutLogEvents
                                - logs:PutResourcePolicy
                                - logs:DescribeResourcePolicies
                                - logs:DescribeLogGroups
                            Resource: "*"
                          - Sid: DLQWrite
                            Effect: Allow
                            Action: sqs:SendMessage
                            Resource:
                                - !GetAtt ValidationDLQ.Arn
                                - !GetAtt PersistenceDLQ.Arn
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # CloudWatch log group for the Step Functions state machine.
    #
    # EXPRESS executions have no built-in console history — the only way
    # to observe what happened inside an execution is CloudWatch Logs.
    # IncludeExecutionData: true means the full input/output of each state
    # is written to the log, which is essential for debugging.
    ProcessingStateMachineLogs:
        Type: AWS::Logs::LogGroup
        Properties:
            LogGroupName: !Sub "/aws/states/streamcore-processing-${Environment}"
            RetentionInDays: 30
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # Validator Lambda — Step 1 of the processing workflow.
    # Receives the full event envelope and raises ValidationError
    # if any field fails the schema or business rules.
    # No environment variables needed here — all config is in code constants.
    ValidatorFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/validator/
            Handler: handler.lambda_handler
            Description: Validates event envelope and type-specific fields; enforces tenant event-type allow-list
            Timeout: 10
            MemorySize: 256
            Environment:
                Variables:
                    TENANT_CONFIG_TABLE: !Ref TenantConfigTable
            Policies:
                - Statement:
                    - Effect: Allow
                      Action: dynamodb:GetItem
                      Resource: !GetAtt TenantConfigTable.Arn

    # PII Extractor Lambda — Step 2 of the processing workflow.
    # Receives the validated event envelope, fetches the per-tenant
    # HMAC-SHA256 salt from Secrets Manager, and replaces every PII
    # field in the payload with its digest. Raw values are never logged
    # or returned.
    PiiExtractorFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/pii_extractor/
            Handler: handler.lambda_handler
            Description: Replaces PII field values with HMAC-SHA256 digests keyed by tenant salt
            Timeout: 10
            MemorySize: 256
            Policies:
                - Statement:
                    - Effect: Allow
                      Action: secretsmanager:GetSecretValue
                      Resource: !Sub "arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:streamcore/pii-salt/*"
                    # kms:Decrypt is required when Secrets Manager retrieves a secret
                    # encrypted with the pipeline CMK. Secrets Manager calls KMS to
                    # decrypt the envelope data key; without this grant the GetSecretValue
                    # call fails with AccessDeniedException (SEC-ENC-02 / SEC-SECRETS-01).
                    - Effect: Allow
                      Action: kms:Decrypt
                      Resource: !GetAtt PipelineKey.Arn

    # Enricher Lambda — Step 3 of the processing workflow.
    # Adds processedAt, pipelineVersion, and schemaVersionUsed to the event.
    # These are pipeline-side facts the client SDK cannot set.
    EnricherFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/enricher/
            Handler: handler.lambda_handler
            Description: Adds processedAt, pipelineVersion, and schemaVersionUsed to the event
            Timeout: 10
            MemorySize: 256
            Environment:
                Variables:
                    PIPELINE_VERSION: "2.0.0"


    # Writer Lambda — Step 4 of the processing workflow.
    # Persists the enriched event to DynamoDB (queryable) and S3 (archive).
    # DynamoDB write is idempotent: attribute_not_exists(PK) condition
    # suppresses duplicate writes on Lambda retries.
    WriterFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/writer/
            Handler: handler.lambda_handler
            Description: Writes processed events to DynamoDB and S3; idempotent via conditional put
            Timeout: 15
            MemorySize: 256
            Environment:
                Variables:
                    TABLE_NAME: !Ref EventsTable
                    EVENTS_BUCKET: !Ref EventsBucket
            Policies:
                - Statement:
                    - Effect: Allow
                      Action: dynamodb:PutItem
                      Resource: !GetAtt EventsTable.Arn
                    - Effect: Allow
                      Action: s3:PutObject
                      Resource: !Sub "${EventsBucket.Arn}/*"

    # Step Functions state machine — orchestrates the four-step processing
    # workflow: validate → hash PII → enrich → persist.
    #
    # Type: STANDARD on LocalStack (enables execution-history API used in
    # tests). Switch to EXPRESS on real AWS (cheaper per-event, < 5 min
    # executions). See ADR-005 for the migration plan and caveats
    # (dedup evaporation on EXPRESS; test coupling to history API).
    #
    # DefinitionSubstitutions injects ARNs and queue URLs from CloudFormation
    # into the ASL at deploy time, replacing ${ValidatorFunctionArn} etc.
    # This keeps references out of the ASL and lets CloudFormation manage them.
    ProcessingStateMachine:
        Type: AWS::Serverless::StateMachine
        Properties:
            DefinitionUri: statemachines/processing.asl.json
            DefinitionSubstitutions:
                ValidatorFunctionArn: !GetAtt ValidatorFunction.Arn
                PiiExtractorFunctionArn: !GetAtt PiiExtractorFunction.Arn
                EnricherFunctionArn: !GetAtt EnricherFunction.Arn
                WriterFunctionArn: !GetAtt WriterFunction.Arn
                ValidationDLQUrl: !Ref ValidationDLQ
                PersistenceDLQUrl: !Ref PersistenceDLQ
            Type: STANDARD
            # Role replaces inline Policies -- SEC-AUTH-04 named dedicated role.
            Role: !GetAtt StreamCoreStepFunctionsRole.Arn
            Logging:
                Destinations:
                    - CloudWatchLogsLogGroup:
                        LogGroupArn: !GetAtt ProcessingStateMachineLogs.Arn
                IncludeExecutionData: true
                Level: ALL
            Tags:
                Project: StreamCore
                Environment: !Ref Environment


    # Durable buffer between the ingestion API and the Consumer Lambda.
    # Kinesis holds events for RetentionPeriodHours, decoupling how fast
    # we accept events from how fast we can process them.
    #
    # Partition key strategy: tenantId (set by the Ingest Lambda on PutRecord).
    # All events from the same tenant land on the same shard, so their relative
    # order is preserved — essential for correct per-tenant event sequencing.
    #
    # Hot-partition note: if one tenant generates dramatically more traffic than
    # others, its shard becomes a bottleneck. Acceptable at this scale; revisit
    # with Application Auto Scaling at higher volumes.
    EventStream:
        Type: AWS::Kinesis::Stream
        Properties:
            Name: !Sub "streamcore-events-${Environment}"
            ShardCount: !Ref ShardCount
            RetentionPeriodHours: !Ref RetentionPeriodHours
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # Raw event archive. Every processed event is written here as a JSON
    # object partitioned by tenantId/eventType/year/month/day/hour/eventId.json.
    # Hour-granularity partitioning lets Athena prune queries to a single
    # hour's prefix without scanning the full bucket, and enables fine-grained
    # replay (re-process one hour of events, not one day).
    #
    # Security: public access is blocked at the bucket level (defence-in-depth
    # — even if a bucket policy is accidentally permissive, this blocks it).
    # Bucket-default AES-256 encryption backs up the per-object SSE in the
    # Writer Lambda — any future put_object that omits ServerSideEncryption
    # is still encrypted.
    #
    # Lifecycle (§7): Standard → IA at 90 d → Glacier at 365 d → delete at 730 d.
    EventsBucket:
        Type: AWS::S3::Bucket
        Properties:
            BucketName: !Sub "streamcore-events-raw-${Environment}"
            PublicAccessBlockConfiguration:
                BlockPublicAcls: true
                BlockPublicPolicy: true
                IgnorePublicAcls: true
                RestrictPublicBuckets: true
            BucketEncryption:
                ServerSideEncryptionConfiguration:
                    - ServerSideEncryptionByDefault:
                          SSEAlgorithm: AES256
            LifecycleConfiguration:
                Rules:
                    - Id: streamcore-event-archive-lifecycle
                      Status: Enabled
                      Transitions:
                          - TransitionInDays: 90
                            StorageClass: STANDARD_IA
                          - TransitionInDays: 365
                            StorageClass: GLACIER
                      ExpirationInDays: 730
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # Bucket policy that enforces SSE on every PutObject (SEC-ENC-03).
    #
    # The condition key s3:x-amz-server-side-encryption is null when the
    # caller omits the header entirely. "Null": true means "this key is
    # absent", so the Deny fires on any put without an SSE declaration.
    #
    # Why this matters alongside the default-encryption setting above:
    # the default silently encrypts objects even when the header is missing.
    # The policy REJECTS those requests instead -- no unencrypted bytes
    # ever reach S3, even transiently.  Defence in depth.
    #
    # Principal: * with Deny -- a Deny always wins over any Allow, so this
    # applies to every caller regardless of their identity, including the
    # WriterFunction role.  WriterFunction already sets AES256 so it is
    # unaffected.
    EventsBucketPolicy:
        Type: AWS::S3::BucketPolicy
        Properties:
            Bucket: !Ref EventsBucket
            PolicyDocument:
                Version: "2012-10-17"
                Statement:
                    - Sid: DenyUnencryptedPuts
                      Effect: Deny
                      Principal: "*"
                      Action: s3:PutObject
                      Resource: !Sub "${EventsBucket.Arn}/*"
                      Condition:
                          "Null":
                              "s3:x-amz-server-side-encryption": "true"

    # Queryable store for processed events.
    #
    # Key design (ADR-007):
    #   PK  = tenantId#eventType    — fast single-partition queries per tenant+type
    #   SK  = ingestedAt#eventId    — stable across retries; eventId is UUID v4,
    #                                  guaranteeing uniqueness within a partition
    #
    # GSI-1 lets you query all event types for a tenant across a time range,
    # e.g. "everything tenant X sent between 09:00 and 10:00".
    #   GSI1PK = tenantId, GSI1SK = ingestedAt
    #
    # TTL on expiresAt: DynamoDB deletes items automatically after 90 days.
    # The Writer Lambda writes expiresAt as a Unix epoch integer.
    # Note: PITR and SSE-KMS are real-AWS hardening — LocalStack ignores them.

    EventsTable:
        Type: AWS::DynamoDB::Table
        Properties:
            TableName: !Sub "streamcore-events-${Environment}"
            BillingMode: PAY_PER_REQUEST   # On-Demand — no capacity planning needed
            AttributeDefinitions:
                - AttributeName: PK
                  AttributeType: S
                - AttributeName: SK
                  AttributeType: S
                - AttributeName: GSI1PK
                  AttributeType: S
                - AttributeName: GSI1SK
                  AttributeType: S
            KeySchema:
                - AttributeName: PK
                  KeyType: HASH
                - AttributeName: SK
                  KeyType: RANGE
            GlobalSecondaryIndexes:
                - IndexName: GSI1
                  KeySchema:
                      - AttributeName: GSI1PK
                        KeyType: HASH
                      - AttributeName: GSI1SK
                        KeyType: RANGE
                  Projection:
                      ProjectionType: ALL
            TimeToLiveSpecification:
                AttributeName: expiresAt
                Enabled: true
            # SSESpecification switches DynamoDB from the default AWS-owned
            # key (aws/dynamodb) to our CMK. DynamoDB calls KMS internally
            # to generate data keys — the Lambda execution roles do NOT need
            # kms:* for table reads/writes (DynamoDB handles that itself).
            # What this buys us: we control the key lifecycle, can revoke
            # access by disabling the key, and every KMS call is in CloudTrail.
            SSESpecification:
                SSEEnabled: true
                SSEType: KMS
                KMSMasterKeyId: !GetAtt PipelineKey.Arn
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # Dead-letter queue for events that fail Step 1 (schema validation).
    #
    # FIFO queue guarantees ordered delivery and exactly-once processing of
    # failed events. ContentBasedDeduplication is off — we will set an
    # explicit MessageDeduplicationId = eventId when sending messages in
    # the state machine Catch blocks (task 3.2). That way SQS drops any
    # duplicate failure for the same event within the 5-minute dedup window.
    #
    # 14-day retention is the SQS maximum, giving ops the largest possible
    # investigation and replay window.
    ValidationDLQ:
        Type: AWS::SQS::Queue
        Properties:
            QueueName: !Sub "streamcore-validation-dlq-${Environment}.fifo"
            FifoQueue: true
            ContentBasedDeduplication: false
            MessageRetentionPeriod: 1209600   # 14 days = 14 × 24 × 3600 seconds
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # ── TenantConfiguration table ─────────────────────────────────────────────
    # Stores per-tenant runtime config: display name, report recipients,
    # permitted event types, PII salt ARN, status.
    #
    # Key design: PK = tenantId (string), no sort key.
    # Every access is GetItem by a known tenant ID — there is no need for a
    # range key or GSI. Keep it simple: one attribute, one operation.
    #
    # No SSE-KMS here — this table holds non-sensitive config, not event data.
    # On-Demand billing matches EventsTable for consistency.
    TenantConfigTable:
        Type: AWS::DynamoDB::Table
        Properties:
            TableName: !Sub "streamcore-tenant-config-${Environment}"
            BillingMode: PAY_PER_REQUEST
            AttributeDefinitions:
                - AttributeName: tenantId
                  AttributeType: S
            KeySchema:
                - AttributeName: tenantId
                  KeyType: HASH
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # Dead-letter queue for events that fail Step 4 (persistence: DynamoDB + S3).
    PersistenceDLQ:
        Type: AWS::SQS::Queue
        Properties:
            QueueName: !Sub "streamcore-persistence-dlq-${Environment}.fifo"
            FifoQueue: true
            ContentBasedDeduplication: false
            MessageRetentionPeriod: 1209600
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # SNS topic that receives CloudWatch alarm state-change notifications (task 3.5).
    # Must be a standard (non-FIFO) topic — CloudWatch alarms cannot publish to
    # FIFO SNS topics. Subscriptions (PagerDuty, email) are Phase 5/6.
    OpsAlertsTopic:
        Type: AWS::SNS::Topic
        Properties:
            TopicName: !Sub "streamcore-ops-alerts-${Environment}"
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # ── CloudWatch Alarms ─────────────────────────────────────────────────────
    # Alert ops when either DLQ accumulates more than 10 messages.
    # ApproximateNumberOfMessagesVisible is the current backlog depth.
    # Maximum over 60 s catches a spike even if it resolves within the period.

    ValidationDLQDepthAlarm:
        Type: AWS::CloudWatch::Alarm
        Properties:
            AlarmName: !Sub "streamcore-validation-dlq-depth-${Environment}"
            AlarmDescription: >
                ValidationDLQ depth exceeded 10 — schema validation failures
                are accumulating. Investigate Validator Lambda errors.
            Namespace: AWS/SQS
            MetricName: ApproximateNumberOfMessagesVisible
            Dimensions:
                - Name: QueueName
                  Value: !GetAtt ValidationDLQ.QueueName
            Statistic: Maximum
            Period: 60
            EvaluationPeriods: 1
            Threshold: 10
            ComparisonOperator: GreaterThanThreshold
            AlarmActions:
                - !Ref OpsAlertsTopic
            OKActions:
                - !Ref OpsAlertsTopic
            TreatMissingData: notBreaching
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    PersistenceDLQDepthAlarm:
        Type: AWS::CloudWatch::Alarm
        Properties:
            AlarmName: !Sub "streamcore-persistence-dlq-depth-${Environment}"
            AlarmDescription: >
                PersistenceDLQ depth exceeded 10 — persistence failures
                are accumulating. Investigate Writer Lambda or DynamoDB errors.
            Namespace: AWS/SQS
            MetricName: ApproximateNumberOfMessagesVisible
            Dimensions:
                - Name: QueueName
                  Value: !GetAtt PersistenceDLQ.QueueName
            Statistic: Maximum
            Period: 60
            EvaluationPeriods: 1
            Threshold: 10
            ComparisonOperator: GreaterThanThreshold
            AlarmActions:
                - !Ref OpsAlertsTopic
            OKActions:
                - !Ref OpsAlertsTopic
            TreatMissingData: notBreaching
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    # Summary Lambda — daily reporting engine.
    # Queries EventsTable GSI-1 (never Scan) for each active tenant and
    # computes the FR-REP-02 aggregate. Triggered by EventBridge (T5.3);
    # publishes to SNS (T5.4); sends SES email (T5.5).
    #
    # Timeout 300 s: in the worst case this queries many tenants with large
    # event volumes. 5 minutes gives ample headroom for LocalStack too.
    #
    # IAM notes:
    #   - dynamodb:Query on EventsTable + its GSI (separate ARNs required)
    #   - dynamodb:Scan on TenantConfigTable: acceptable for a small admin
    #     table; see worklog for deviation from spec's GetItem-only wording
    SummaryFunction:
        Type: AWS::Serverless::Function
        Properties:
            CodeUri: functions/summary/
            Handler: handler.lambda_handler
            Description: Aggregates previous day events per tenant for daily reports
            Timeout: 300
            MemorySize: 512
            Environment:
                Variables:
                    EVENTS_TABLE:        !Ref EventsTable
                    EVENTS_GSI:          GSI1
                    TENANT_CONFIG_TABLE: !Ref TenantConfigTable
                    REPORTS_TOPIC_ARN:   !Ref DailyReportsTopic
                    SENDER_EMAIL:        reports@streamcore.io
                    OPS_EMAIL:           ops@streamcore.io
            Policies:
                - Statement:
                    - Effect: Allow
                      Action: dynamodb:Query
                      Resource:
                          - !GetAtt EventsTable.Arn
                          - !Sub "${EventsTable.Arn}/index/*"
                    - Effect: Allow
                      Action:
                          - dynamodb:Scan
                          - dynamodb:GetItem
                      Resource: !GetAtt TenantConfigTable.Arn
                    - Effect: Allow
                      Action: sns:Publish
                      Resource: !Ref DailyReportsTopic
                    - Effect: Allow
                      Action:
                          - ses:SendEmail
                          - ses:SendRawEmail
                      Resource: "*"

    # ── Summary schedule DLQ ────────────────────────────────────────────────
    SummaryScheduleDLQ:
        Type: AWS::SQS::Queue
        Properties:
            QueueName: !Sub "streamcore-summary-schedule-dlq-${Environment}"
            MessageRetentionPeriod: 1209600
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

    SummaryScheduleDLQPolicy:
        Type: AWS::SQS::QueuePolicy
        Properties:
            Queues:
                - !Ref SummaryScheduleDLQ
            PolicyDocument:
                Version: "2012-10-17"
                Statement:
                    - Effect: Allow
                      Principal:
                          Service: events.amazonaws.com
                      Action: sqs:SendMessage
                      Resource: !GetAtt SummaryScheduleDLQ.Arn
                      Condition:
                          ArnEquals:
                              aws:SourceArn: !GetAtt SummaryScheduleRule.Arn

    # ── EventBridge scheduled rule ──────────────────────────────────────────
    SummaryScheduleRule:
        Type: AWS::Events::Rule
        Properties:
            Name: !Sub "streamcore-${Environment}-summary-daily"
            Description: "Triggers SummaryFunction daily at 07:00 UTC"
            ScheduleExpression: "cron(0 7 * * ? *)"
            State: ENABLED
            Targets:
                - Id: SummaryLambdaTarget
                  Arn: !GetAtt SummaryFunction.Arn
                  DeadLetterConfig:
                      Arn: !GetAtt SummaryScheduleDLQ.Arn

    SummaryLambdaInvokePermission:
        Type: AWS::Lambda::Permission
        Properties:
            FunctionName: !Ref SummaryFunction
            Action: lambda:InvokeFunction
            Principal: events.amazonaws.com
            SourceArn: !GetAtt SummaryScheduleRule.Arn

    # ── Daily reports SNS topic ─────────────────────────────────────────────
    DailyReportsTopic:
        Type: AWS::SNS::Topic
        Properties:
            TopicName: !Sub "streamcore-daily-reports-${Environment}"
            Tags:
                - Key: Project
                  Value: StreamCore
                - Key: Environment
                  Value: !Ref Environment

Outputs:
    ProcessingStateMachineArn:
        Description: ARN of the event processing state machine
        Value: !GetAtt ProcessingStateMachine.Arn
    ValidationDLQUrl:
        Description: URL of the validation dead-letter queue
        Value: !Ref ValidationDLQ
    ValidationDLQArn:
        Description: ARN of the validation dead-letter queue
        Value: !GetAtt ValidationDLQ.Arn
    PersistenceDLQUrl:
        Description: URL of the persistence dead-letter queue
        Value: !Ref PersistenceDLQ
    PersistenceDLQArn:
        Description: ARN of the persistence dead-letter queue
        Value: !GetAtt PersistenceDLQ.Arn
    OpsAlertsTopicArn:
        Description: ARN of the ops alerts SNS topic
        Value: !Ref OpsAlertsTopic
    SummaryFunctionArn:
        Description: ARN of the Summary Lambda (EventBridge schedule target)
        Value: !GetAtt SummaryFunction.Arn
    SummaryScheduleRuleName:
        Description: EventBridge rule that fires the Summary Lambda daily
        Value: !Ref SummaryScheduleRule
    DailyReportsTopicArn:
        Description: SNS topic for per-tenant daily report fan-out
        Value: !Ref DailyReportsTopic
